import 'package:flutter/material.dart';

class DataStorageEmpyWidget extends StatelessWidget {
  const DataStorageEmpyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

enum NavPosition {
  top,
  bottom,
}

enum Categories {
  sort,
  contains,
  dateCreated,
  dateModified,
  app,
  atsign,
  keyTypes,
  sharedWith,
  sharedBy,
}

enum SortOptions {
  ascending,
  descending,
}

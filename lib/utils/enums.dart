enum NavPosition {
  top,
  bottom,
}

enum Categories {
  sort,
  contains,
  dateCreated,
  dateModified,
  apps,
  atsign,
  keyTypes,
  sharedWith,
  sharedBy,
}

enum SortOptions {
  ascending,
  descending,
}

enum SearchWidgetFilter { apps, atSigns }

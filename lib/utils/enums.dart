enum NavPosition {
  top,
  bottom,
}

enum Categories {
  contains,
  sort,
  dateCreated,
  dateModified,
  namespaces,
  atsign,
  keyTypes,
  sharedWith,
  sharedBy,
}

enum SortOptions {
  ascending,
  descending,
}

enum SearchWidgetFilter { apps, atSigns }
